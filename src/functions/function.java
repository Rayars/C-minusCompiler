package functions;

public class function {
    public static boolean isalpha(char c){
        if(Character.isLetter(c)){
            return true;
        }else{
            return false;
        }
    }
}
